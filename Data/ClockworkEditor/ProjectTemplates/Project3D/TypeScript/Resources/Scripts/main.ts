// This script is the main entry point of the game

var scene = Clockwork.player.loadScene("Scenes/Scene.scene");

// called per frame, optional
export function update(timeStep) {
    // Handle update
}
